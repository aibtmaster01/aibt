# 모바일·반응형 수동 QA 시나리오

공통 컴포넌트(`MobilePageHeader`, `MobileBottomActionBar`, `MobileSheet`, `MobileFullScreenModal`, `ResponsivePageContainer`, `SectionAccordion`, `FlashToast`)와 스크롤·모달 정책 반영 후 회귀 검증용입니다.

## 뷰포트 정의

| 구분 | 대략 폭 | DevTools 프리셋 예시 |
|------|---------|----------------------|
| iPhone SE | 375×667 | iPhone SE |
| 일반 모바일 | 390~430× 높이 가변 | iPhone 14 / Pixel 7 |
| 태블릿 세로 | 768~834 | iPad Mini 세로 |
| 데스크톱 | ≥1024 (lg 브레이크포인트) | 일반 노트북 창 |

**iOS Safari 추가:** 주소창 표시/숨김으로 스크롤 후 하단 고정 바·시트·토스트가 잘리지 않는지 확인합니다.

---

## 회원 유형별 핵심 플로우

### 게스트

1. 랜딩 또는 시험 목록에서 **게스트로 모의고사 시작** → 퀴즈 진입.
2. **20문항 제한** 도달 시 안내·로그인/가입 유도 확인.
3. 결과 화면: **상단 요약(점수·피드백·CTA)** 가 먼저 보이고, 상세 리포트는 **블러/잠금** 처리되는지.
4. 로그인 유도 CTA 동작.

### 무료 회원

1. 로그인 후 자격증 선택 → **1·2회차** 학습·결과까지 완료.
2. 결과에서 **다음 학습 / 결제 유도** (제품 정책에 맞는 버튼·모달) 노출.
3. 마이페이지: **학습 시작**, **과목 강화** 등 무료 가능 기능 / **열공 전용** 잠금·안내.
4. 퀴즈 **문제 신고**: 로그인 시 전면 모달·전송 후 **토스트** (중앙 `alert` 없음).

### 유료(열공) 회원

1. 잠금 없이 **취약 유형·개념 집중** 등 프리미엄 플로우 진입.
2. 결과: **과목·오답·전체 보기** 아코디언 펼침 시 **페이지 단일 스크롤**만 사용되는지 (내부 `max-h` 스크롤 난립 없음).
3. 만료 배너·결제 연장 UI가 레이아웃을 깨지 않는지.

---

## 화면별 체크리스트

### 앱 셸 · 시험 목록

| 뷰포트 | 확인 사항 |
|--------|-----------|
| iPhone SE | 좁은 폭에서 제목·버튼 줄바꿈, 터치 영역 ≥44px |
| 모바일 | 사이드바 없이 콘텐츠만 스크롤, 하단 네비와 겹침 없음 |
| 태블릿 세로 | 한 열 레이아웃 가독성, 과도한 여백 없음 |
| 데스크톱 | 사이드바 + 본문 2단, 스크롤 영역 명확 |

### 퀴즈 (`Quiz`)

| 뷰포트 | 확인 사항 |
|--------|-----------|
| 전체 | `MobilePageHeader` / `MobileBottomActionBar`·시트 열림 시 하단 바 숨김 규칙 |
| 모바일 | 문항 그리드·메모: **바텀 시트**; 종료 확인·약점 끝: **시트** (가벼운 확인) |
| 모바일 | **신고**: `MobileFullScreenModal` + 하단 푸터 버튼, 본문만 스크롤 |
| 모바일 | 신고 성공/실패: **FlashToast** (하단 safe-area) |
| 데스크톱 | 동일 플로우가 **중앙 카드**로 표시되는지 |

### 결과 (`Result`)

| 뷰포트 | 확인 사항 |
|--------|-----------|
| 모바일 | `ResponsivePageContainer` 기준 **1열 스크롤**; 상단: 목록/대시보드, 그다음 **점수·피드백·주 CTA** |
| 모바일 | `SectionAccordion`: 과목·개념 / 메모 / 오답 / 전체 보기 — 펼쳐도 **중첩 스크롤 없이** 길게 스크롤 |
| 모바일 | 하단 고정 **다시 풀기**와 본문 패딩(`safe-area`) |
| 태블릿 | 요약 카드 + 접힌 상세 균형 |
| 데스크톱 | 기존 넓은 카드·표 레이아웃 유지 |

### 마이페이지 (`MyPage_beta`)

| 뷰포트 | 확인 사항 |
|--------|-----------|
| 모바일 | `ResponsivePageContainer` + 학습 홈 카드 → **바로 행동** 그리드 → `SectionAccordion` (분석·기록) 순서 |
| 모바일 | 분석·기록 펼침 시 긴 목록도 **페이지 스크롤만** |
| 태블릿·데스크톱 | `lg` 레이아웃과 충돌 없음 |

---

## 짧은 회귀 스모크 (전 플랫폼)

1. 퀴즈 중 시트 연 후 닫기·뒤로가기(종료 확인).
2. iOS에서 키보드가 뜨는 입력(있다면) 후 레이아웃.
3. `Escape`로 시트/모달 닫기(데스크톱).
4. `npm run build` 성공 상태 유지.

---

문서 버전: IA 공통 컴포넌트·모달 정책 정리 반영 (2026-03).
