# 브랜드/기능 (복사해 .env.production / .env.beta 로 사용)
# 실서버: .env.production 또는 생략 시 기본값(핀셋) 적용
# 베타: .env.beta 생성 후 아래 두 줄 넣고 npm run build:beta
#   VITE_APP_BRAND=AiBT
#   VITE_FEATURE_COUPON=true

VITE_APP_BRAND=핀셋
VITE_FEATURE_COUPON=false
